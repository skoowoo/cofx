load "go:print"
load "go:time"

// use variable to print 'hello world!'
var a = "hello"
var b = "$(a) world!"

co print {
    "_" : "$(b)"
}

// print current time
var out
co time -> out
co print {
    "_": "$(out.Now)"
}